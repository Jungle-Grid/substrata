[project]
name = "substrata-classifier"
version = "0.1.0"
description = "Deterministic MVP worker for Substrata datasheet classification."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

